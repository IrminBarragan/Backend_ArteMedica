# Los tests corren contra H2 en memoria para no depender de un PostgreSQL levantado.
spring.datasource.url=jdbc:h2:mem:artemedica;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

app.jwt.secret=secreto-solo-para-pruebas-lo-bastante-largo-para-hmac-sha-512-abcdefghijklmnop
app.jwt.expiration-ms=3600000

app.cors.allowed-origins=http://localhost:5173

app.seed.admin.username=admin
app.seed.admin.password=passwordDePrueba
app.seed.demo.enabled=false
